<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_inherit_birth_date" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.birth_date</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

        <xpath expr="//div[hasclass('d-flex', 'align-items-baseline', 'w-md-50')][2]" position="after">
            <div class="d-flex align-items-baseline w-md-50">
                <i class="fa fa-fw me-1 fa-mobile text-primary" title="Mobile"/>
                <field name="mobile" class="w-100" widget="phone" placeholder="Celular"/>
            </div>
        </xpath>

        <xpath expr="//group[1]" position="inside">
            <group>
                <field name="birth_date"/>
                <field name="card_code"/>
            </group>
        </xpath>
        </field>
    </record>
</odoo>
