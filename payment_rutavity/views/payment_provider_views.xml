<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">Rutavity Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position="inside">
                <group invisible="code != 'rutavity'">
                    <field name="epayco_cust_id" required="code == 'rutavity' and state != 'disabled'"/>
                    <field name="epayco_public_key" required="code == 'rutavity' and state != 'disabled'" password="True"/>
                    <field name="epayco_private_key" required="code == 'rutavity' and state != 'disabled'" password="True"/>
                    <field name="epayco_p_key" required="code == 'rutavity' and state != 'disabled'" password="True"/>
                </group>
            </group>
        </field>
    </record>

</odoo>
